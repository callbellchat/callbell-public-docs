```python
import requests

headers = {
    'Authorization': 'Bearer test_gshuPaZoeEG6ovbc8M79w0QyM',
    'Content-Type': 'application/json',
}

json_data = {
    'name': 'Lunch',
}

response = requests.post('https://api.callbell.eu/v1/custom_statuses', headers=headers, json=json_data)

# Note: json_data will not be serialized by requests
# exactly as it was in the original request.
#data = '{\n        "name": "Lunch"\n    }'
#response = requests.post('https://api.callbell.eu/v1/custom_statuses', headers=headers, data=data)

```