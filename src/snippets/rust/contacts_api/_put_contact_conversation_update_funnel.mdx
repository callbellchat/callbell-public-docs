```rust
extern crate reqwest;
use reqwest::header;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let mut headers = header::HeaderMap::new();
    headers.insert("Authorization", "Bearer test_gshuPaZoeEG6ovbc8M79w0QyM".parse().unwrap());
    headers.insert("Content-Type", "application/json".parse().unwrap());

    let client = reqwest::blocking::Client::builder()
        .redirect(reqwest::redirect::Policy::none())
        .build()
        .unwrap();
    let res = client.post("https://api.callbell.eu/v1/contacts/efd7d996470e463b89f079be6cf578ed/messages/update_funnel")
        .headers(headers)
        .body(r#"
{
        "funnel_id": "ff0f96b9-b003-4d0c-b14f-27f3f6868dd6",
    }
"#
        )
        .send()?
        .text()?;
    println!("{}", res);

    Ok(())
}

```